version: '2'
services:
  nginx:
    image: ${NGINX_IMAGE}
    hostname: nginx
    volumes:
      - ./etc:/etc/nginx/conf.d
      - ./certs:/etc/nginx/certs
      - ${NGINX_LOG_DIR}:/var/log/nginx
    ports:
      - "${VM1_VXLAN_IP}:${NGINX_PORT}:443"
